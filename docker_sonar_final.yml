version: '3'

services:
  sonarqube:
    build:
      context: .
      dockerfile: Dockerfile.sonarqube-lts
    container_name: sonarqube-final
    environment:
      - sonar.jdbc.username=sonar
      - sonar.jdbc.password=sonar
      - sonar.jdbc.url=jdbc:postgresql://sonarqube-postgres:5432/sonar
      - SONAR_WEB_JAVAADDITIONALOPTS=-javaagent:./extensions/plugins/sonarqube-community-branch-plugin-1.14.0.jar=web
      - SONAR_CE_JAVAADDITIONALOPTS=-javaagent:./extensions/plugins/sonarqube-community-branch-plugin-1.14.0.jar=ce
    volumes:
      - sonar_data_final:/opt/sonarqube/data:rw
      - sonar_ext_final:/opt/sonarqube/extensions:rw
    ports:
      - "9000:9000"
    depends_on:
      - sonarqube-postgres
    networks:
      - sonarnet

  sonarqube-postgres:
    image: postgres:15.6
    container_name: sonarqube-postgres-final
    environment:
      - POSTGRES_USER=sonar
      - POSTGRES_PASSWORD=sonar
      - POSTGRES_DB=sonar
    volumes:
      - sonar_data_db_final:/var/lib/postgresql/data:rw
    networks:
      - sonarnet

volumes:
  sonar_data_final:
  sonar_ext_final:
  sonar_data_db_final:

networks:
  sonarnet:
    driver: bridge
